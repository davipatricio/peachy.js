const Requester = require('../utils/Requester');

class TextChannel {
	constructor (client, data) {
		this.client = client;
		this.parseData(data);
	}

	parseData (data) {
	}
}

module.exports = TextChannel;
